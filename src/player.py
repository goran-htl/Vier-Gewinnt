# Furkan Gueyen
class Player:
    def __init__(self, name, piece):
        self.name = name  # Initialisiert den Namen des Spielers
        self.piece = piece  # Initialisiert das Spielstück (z.B. 'X' oder 'O')